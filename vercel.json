{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["templates/**", "static/**", "resources/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.ts" }
  ]
}